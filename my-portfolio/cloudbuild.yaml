# Cloud Build configuration for automatic deployment from GitHub
steps:
  # Convert service name to lowercase for Docker image (Docker requires lowercase)
  - name: 'bash'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        # Convert _SERVICE_NAME to lowercase and save to file
        # Use _IMAGE_NAME if provided, otherwise convert _SERVICE_NAME
        if [ -n "$${_IMAGE_NAME:-}" ]; then
          echo "$${_IMAGE_NAME}" | tr '[:upper:]' '[:lower:]' > /workspace/image-name.txt
        else
          echo "$${_SERVICE_NAME}" | tr '[:upper:]' '[:lower:]' > /workspace/image-name.txt
        fi
        echo "Image name: $$(cat /workspace/image-name.txt)"
  
  # Build the Docker image (using lowercase image name)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/$$(cat /workspace/image-name.txt)'
      - '.'
  
  # Push the image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/$$(cat /workspace/image-name.txt)'
  
  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '$_SERVICE_NAME'
      - '--image'
      - 'gcr.io/$PROJECT_ID/$$(cat /workspace/image-name.txt)'
      - '--region'
      - '$_REGION'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '3000'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'

# Store images in Container Registry
images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}'

# Build options
options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
